# FreshChain ML Service — Python Dependencies
#
# Install: pip install -r requirements.txt
# Dev:     pip install -r requirements.txt pytest pytest-mock pytest-asyncio httpx

# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0         # includes websockets + httptools for speed
python-multipart>=0.0.9           # required for UploadFile support

# ── ML ───────────────────────────────────────────────────────────────────────
transformers>=4.40.0
# CPU-only torch — keeps the Docker image under 2 GB for Render free tier.
# For GPU on Modal/HF Spaces Pro, replace with: torch>=2.2.0+cu121 --extra-index-url ...
torch>=2.2.0
Pillow>=10.3.0                    # image decode + resize

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx>=0.27.0                     # async HTTP for image_service

# ── Config ───────────────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.2.0          # BaseSettings + .env loading

# ── Dev / test (not needed in production) ────────────────────────────────────
# pytest>=8.2.0
# pytest-mock>=3.14.0
# pytest-asyncio>=0.23.0
